# Core Module

This directory contains singleton services and features that are loaded once when the application starts.

Contents:
- `/services` - Global singleton services
- `/guards` - Route guards
- `/interceptors` - HTTP interceptors